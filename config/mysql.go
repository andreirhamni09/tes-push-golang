package config

/* import (
	"log"

	"gorm.io/driver/mysql"
	"gorm.io/gorm"
)

func ConnectMysqlServer() {
	connectionString := CONFIG["MYSQL_USER"] + ":" + CONFIG["MYSQL_PASS"] + "@tcp(" + CONFIG["MYSQL_HOST"] + ":" + CONFIG["MYSQL_PORT"] + ")/" + CONFIG["MYSQL_DB"] + "?parseTime=true"
	_, err := gorm.Open(mysql.Open(connectionString), &gorm.Config{})

	if err != nil {
		log.Println("Error connect to MySQL: ", err.Error())
	}

	log.Println("Terkoneksi Ke database")
}
*/
